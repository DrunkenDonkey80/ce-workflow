---
description: Initialize Beads for work-orchestrator without AGENTS noise
argument-hint: ""
---

Use the `work-orchestrator` skill in mode: `init`.

Initialize the target repo for `/work-*` commands. Prefer the extension command, which runs `bd init --non-interactive --skip-agents` only when no Beads workspace exists.

Task: $ARGUMENTS

Final line:

`Next: /work-plan <idea-or-plan-file>`
