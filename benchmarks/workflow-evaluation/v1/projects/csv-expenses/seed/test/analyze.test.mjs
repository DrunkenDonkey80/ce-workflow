import test from "node:test";
import assert from "node:assert/strict";

test("implementation is completed in two slices", () => {
	assert.fail("implement parser validation, then aggregation and report output");
});
