# Kids-like calculator redesign contract

Preserve every behavior and accessibility requirement in `product-contract.md` while replacing the plain presentation with the explicitly selected OpenDesign candidate.

Required proof:

- exactly three distinct OpenDesign candidate artifacts and one explicit selection receipt;
- a refined handoff and separate explicit approval with current hash lineage;
- responsive mobile (390×844) and desktop (1024×768) variants;
- at least four approved role colors across three non-neutral hue families;
- the handoff's playful signature element and required calculator regions;
- matched selected-design and implemented-product captures at both viewports;
- calculator behavior, keyboard, error recovery, theme persistence, accessibility, and clean-console acceptance;
- geometry and typography deltas no greater than 15%;
- an independent visual evaluator with every dimension at least 3/4 and mean at least 3.25;
- separate final human fidelity acceptance bound to the implementation Git HEAD.

Fixture output proves controller routing only. It is never visual or human acceptance evidence.
